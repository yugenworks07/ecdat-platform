import type { LucideIcon } from "lucide-react";

export function MetricCard({ label, value, note, icon: Icon, tone = "cyan", onClick }: { label: string; value: string | number; note: string; icon: LucideIcon; tone?: "cyan" | "rose" | "amber" | "emerald" | "violet"; onClick?: () => void }) {
  const tones = { cyan: "from-cyan-300/15 to-cyan-300/0 text-cyan-100 ring-cyan-200/15", rose: "from-rose-300/15 to-rose-300/0 text-rose-100 ring-rose-200/15", amber: "from-amber-200/15 to-amber-200/0 text-amber-100 ring-amber-200/15", emerald: "from-emerald-300/15 to-emerald-300/0 text-emerald-100 ring-emerald-200/15", violet: "from-violet-300/15 to-violet-300/0 text-violet-100 ring-violet-200/15" };
  const content = <><span className="absolute inset-y-4 left-0 w-0.5 rounded-r-full bg-current opacity-70" /><Icon className="absolute right-4 top-4 h-5 w-5 opacity-55" /><p className="text-[10px] font-semibold uppercase tracking-[0.14em] text-slate-500">{label}</p><p className="mt-3 font-display text-3xl font-semibold tracking-[-0.04em] text-white">{value}</p><p className="mt-1 text-xs text-slate-400">{note}</p>{onClick ? <span className="mt-3 block text-[10px] font-medium text-current/75">View context →</span> : null}</>;
  const className = `relative overflow-hidden rounded-2xl bg-gradient-to-br p-5 text-left ring-1 ${tones[tone]} ${onClick ? "cursor-pointer transition hover:-translate-y-0.5 hover:brightness-110 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-cyan-200/60 active:scale-[0.99]" : ""}`;
  return onClick ? <button type="button" onClick={onClick} className={className}>{content}</button> : <div className={className}>{content}</div>;
}
